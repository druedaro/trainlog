# Groq API key for Whisper transcription (server-side only)
GROQ_API_KEY=

# Google Gemini API key (server-side only)
GOOGLE_GENAI_API_KEY=

# Firebase Admin SDK service account (base64 encoded, server-side only)
FIREBASE_SERVICE_ACCOUNT=
